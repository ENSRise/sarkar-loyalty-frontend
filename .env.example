# ── API ───────────────────────────────────────────────────────────────
# Base URL of the backend (used by ReferralJoin public page)
# In development the Vite proxy rewrites /api → localhost:3000, so leave this blank
VITE_API_URL=
